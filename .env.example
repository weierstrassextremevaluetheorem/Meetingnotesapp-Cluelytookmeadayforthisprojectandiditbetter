# All settings can also be configured via the in-app Settings panel.
# Environment variables are used as fallbacks when no setting is saved.

# ── OpenAI (transcription + notes) ──────────────────────────
OPENAI_API_KEY=sk-your-key-here
TRANSCRIPTION_MODEL=gpt-4o-transcribe
LLM_MODEL=gpt-4o
LLM_ENDPOINT=https://api.openai.com/v1/chat/completions

# ── Deepgram (alternative transcription provider) ───────────
# DEEPGRAM_API_KEY=your-deepgram-key

# ── Anthropic (alternative notes provider) ──────────────────
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Integrations (optional) ─────────────────────────────────
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../xxx
# NOTION_API_KEY=ntn_...
# NOTION_DATABASE_ID=your-database-id

# ── Data retention (optional, in days) ──────────────────────
# RETENTION_DAYS=90
